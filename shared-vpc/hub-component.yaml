version: 1
kind: component

requires:
  - terraform

parameters:
  - name: sharedVpc
    parameters:
      - name: subnetwork
      - name: hostProjectId
      - name: serviceProjectId

templates:
  files:
    - "*.template"
